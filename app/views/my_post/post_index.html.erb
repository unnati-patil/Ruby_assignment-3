<p id = "notice" ><%= notice %></p>

<h3>Welcome !!!</h3>

<%= link_to 'Add user', post_add_my_post_index_path %>

<br> <br>

<table align = "left" border = "1" cellpadding = "1">
  <tr>
    <th> Name        </th>
    <th>Action1 </th>
    <th>Action2 </th>
    <th>Action3 </th>
  </tr>

  <% @my_posts.each do |post|%>
      <tr>
        <td><%= post.name %></td>

        <td><%= link_to 'Show user',post_show_my_post_path(post)%> </td>
        <td><%= link_to 'Edit user',post_edit_my_post_path(post)%> </td>
        <td><%= link_to 'Delete user',post_delete_my_post_path(post), :method => :delete%> </td>

      </tr>
  <% end %>

</table>
<br><br><br><br><br><br><br><br><br><br><br><br>

<%= will_paginate @my_posts %>